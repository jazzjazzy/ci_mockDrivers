{
  "name": "mockdrivers",
  "version": "1.0.0",
  "description": "testing mock driver, will read in gpx file and send to location server via websockets",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^6.1.3",
    "xml2json": "^0.11.2"
  }
}
